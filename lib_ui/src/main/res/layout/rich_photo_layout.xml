<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/rich_photo_tv_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/rich_photo_iv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:contentDescription="@string/photo"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/rich_photo_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:background="@color/transparent_black"
            android:gravity="center"
            android:letterSpacing="0.05"
            android:paddingTop="@dimen/texts_padding"
            android:paddingBottom="@dimen/texts_padding"
            android:textColor="@color/white"
            android:textSize="@dimen/sub_text"
            app:layout_constraintTop_toTopOf="parent" />

        <FrameLayout
            android:id="@+id/rich_photo_detail_container"
            android:layout_width="match_parent"
            android:layout_height="@dimen/list_icon"
            android:background="@color/transparent_black"
            android:paddingTop="@dimen/texts_padding"
            android:paddingBottom="@dimen/texts_padding"
            app:layout_constraintBottom_toBottomOf="parent">

            <TextView
                android:id="@+id/rich_photo_detail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:letterSpacing="0.05"
                android:textColor="@color/white"
                android:textSize="@dimen/sub_text" />

            <ImageView
                android:id="@+id/rich_photo_full"
                android:layout_width="@dimen/action_icon"
                android:layout_height="@dimen/action_icon"
                android:layout_gravity="end|center_vertical"
                android:layout_marginEnd="@dimen/main_margin"
                android:contentDescription="@string/action"
                android:src="@drawable/ic_baseline_fullscreen_24_white" />
        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
